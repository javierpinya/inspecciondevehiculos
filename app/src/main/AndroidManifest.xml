<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.javierpinya.inspecciondevehiculos">

    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/clh_logo"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <uses-library android:name="org.apache.http.legacy" android:required="false"/>
        <activity android:name=".activities.BuscarChipActivity"></activity>
        <activity android:name=".activities.BuscarSimacActivity" />
        <activity android:name=".activities.BuscarTodoActivity" />
        <activity android:name=".activities.BuscarTransportistaActivity" />
        <activity android:name=".activities.SincronizarActivity" />
        <activity android:name=".activities.ValoracionInspeccionActivity" />
        <activity android:name=".activities.CompartimentosInspeccionActivity" />
        <activity android:name=".activities.CabeceraInspeccionActivity" />
        <activity android:name=".activities.FotoActivity" />
        <activity android:name=".activities.CompartimentosActivity" />
        <activity android:name=".activities.ResultadoBuscarVehiculoActivity" />
        <activity android:name=".activities.ResultadoInspeccionActivity" />
        <activity android:name=".activities.BuscarInspeccionActivity" />
        <activity android:name=".activities.BuscarVehiculoActivity" />
        <activity
            android:name=".activities.MenuActivity"
            android:label="@string/title_activity_menu" />
        <activity android:name=".activities.MainActivity" />
        <activity android:name=".activities.LoginActivity" />
        <activity
            android:name=".Splash.SplashActivity"
            android:theme="@style/SplashScreen">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>